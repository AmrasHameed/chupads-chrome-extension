{
  "name": "ad-blocker",
  "version": "1.1.0",
  "main": "index.js",
  "scripts": {
    "build": "node scripts/build-bundled-rules.js",
    "compile:rules": "npx ts-node compiler/index.ts easylist.txt rules/static-rules.json",
    "package": "node scripts/package.js",
    "release": "npm run build && npm run package",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^6.0.3"
  }
}
